a hard copy of [yourls](http://yourls.org/) implemented by [nodejs](http://nodejs.org)

to install all dependencies:

* npm install express
* npm install jade
* npm install mongodb
* npm install underscore
* npm install cluster
